import {makeStyles} from '@material-ui/styles';

export default makeStyles(() => ({
  btnWrapper: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-around',
    marginTop: '5%',
  },
}))
;
